import Order from '../models/order.model'

// Helper function to format order details
function formatOrderDetails(order) {
    // Implement this function to return a formatted string of order details
    // You might want to create an HTML table for the HTML version
    // and a plain text version for the text version
  }